import { HeightlineDirective } from './heightline.directive';

describe('HeightlineDirective', () => {
  it('should create an instance', () => {
    const directive = new HeightlineDirective();
    expect(directive).toBeTruthy();
  });
});
